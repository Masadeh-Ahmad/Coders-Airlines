@page
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model Coders_Airlines.Pages.Cars.CarRentalModel
@{
}
<img src="@Model.Car.Thumbnail" width="150" height="100">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label  class="control-label">From</label>
                <input asp-for="@Model.CarRental.From" class="form-control" />
                
            </div>
            <div class="form-group">
                <label  class="control-label">To</label>
                <input asp-for="@Model.CarRental.To" class="form-control" />

            </div>
            <input type="submit" value="Check" class="btn btn-primary" asp-page-handler="check" asp-route-id="@Model.Car.ID" />
        
        @if(Model.CarRental != null)
        {
            if(Model.CarRental.Price !=0)
            {


                <label asp-for="@Model.CarRental.Price" class="control-label"></label>
                <span>"@Model.CarRental.Price" </span>
                 
                <input type="submit" value="Rent" class="btn btn-primary" asp-route-id="@Model.Car.ID" />

            
            }
            else
            {
            <span>This Car is not available at this time </span>
            }
        }
        </form>
       
        
